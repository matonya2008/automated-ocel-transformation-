# Python dependencies for the code-availability package.
# NOTE: You must ALSO install the Graphviz C binaries separately
#       (https://graphviz.org/download/) and ensure `dot` is on your PATH.
#       On Windows this is required for both pm4py and the graphviz Python package.

matplotlib>=3.8
numpy>=1.26
pandas>=2.2
pm4py>=2.7
graphviz>=0.20
scikit-learn>=1.5
seaborn>=0.13
xgboost>=2.1
